import {
    StyleSheet,
} from 'react-native';

export const GlobalStyles = StyleSheet.create({
    main: {
        display: 'flex',
        flexDirection: 'column',
        backgroundColor: '#fff',
        width: '100%',
        height: '100%',
        padding: 0,
        margin: 0,
    },
});
